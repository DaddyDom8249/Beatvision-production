import {describe,it,expect} from "vitest";
describe("BeatVision contracts",()=>{it("keeps Pixazo prompt within documented limit",()=>expect("x".repeat(3000).slice(0,2048)).toHaveLength(2048));it("requires world fields",()=>{const r:any={song_summary:"x"};expect(["emotional_arc","visual_language","environments","characters","style_bible"].filter(k=>!(k in r))).toHaveLength(5)})});